#!/usr/bin/env bash
# Mock ffmpeg used by end-to-end tests: creates an empty file at the output
# path (the last argument) instead of transcoding audio.

set -e

OUTPUT="${*: -1}"
mkdir -p "$(dirname "$OUTPUT")"
: > "$OUTPUT"
